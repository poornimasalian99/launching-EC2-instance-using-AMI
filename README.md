# launching-EC2-instance-using-AMI
Here firstly, created a ec2 instance and got a output using public ip. Then stopped the instance and created a AMI image . Then Launch ec2 instance from created AMI . Then got output from public Ip address.
